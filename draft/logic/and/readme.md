# and(...conditions)

Returns true if all conditions are true, false otherwise.

## Usage

```js
import and from 'doremi/logic/and';

and(true, true, true);
// => true

and(true, false, true);
// => false
```