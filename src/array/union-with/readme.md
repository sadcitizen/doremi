# unionWith(...arrays, [comparator])

Unites the arrays.

## Usage

```js
import unionWith from 'doremi/array/union-with';

const a = ['a', 'b', 'c'];
const b = ['d', 'e', 'f'];

unionWith(a, b);
// => ['a', 'b', 'c', 'd', 'e', 'f']
```
