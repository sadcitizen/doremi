/**
 * Reverses a string.
 *
 * @category string
 * @param {string} value The string to reverse.
 * @returns {string} Returns the reversed string.
 */
export default value => String(value).split('').reverse().join('');
