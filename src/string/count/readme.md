# count(value, substring): number

Counts the number of occurrences of a substring in the given string.

## Usage

```js
import count from 'doremi/string/count';

count('Hello, World!', 'l');
// => 3

count('Hello, World!', 'll');
// => 1

```
