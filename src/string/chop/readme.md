# chop(value, [step]): array

Chops the string into pieces with length equal `step` from left to right.

## Usage

```js
import chop from 'doremi/string/chop';

chop('lorem ipsum', 5);
// => ['lorem', ' ipsu', 'm']
```
