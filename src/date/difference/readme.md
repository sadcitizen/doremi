# difference(value, other, unit)

Calculates the difference between the dates.

## Usage

```js
const value = new Date(2000, 0);
const other = new Date(2001, 0);

difference(date, other, 'month'); // => 12
```
