# isNumber(num)

Checks if the value is a number.

## Usage

```js
import isNumber from 'doremi/number/is-number';

isNumber(false);
// => false

isNumber(42);
// => true

isNumber('42');
// => false
```