import compose from './compose';
import partial from './partial';
import pipe from './pipe';

export { compose };
export { partial };
export { pipe };

export default {
    compose,
    partial,
    pipe
};
