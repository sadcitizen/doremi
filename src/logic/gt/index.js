/**
 * Checks if the value is greater than the other value.
 *
 * @category logic
 * @param {number} value The value to compare.
 * @param {number} other The other value to compare
 * @returns {boolean} Returns `true` if the value is greater than the other value, else `false`.
 */
export default (value, other) => value > other;
